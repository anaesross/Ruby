source 'https://rubygems.org' #onde geralmente as gems são baixadas

gem 'os'
gem 'rake'

#para executar o arquivo gemfile e instalar todas as gems listadas, executar no terminal = bundle
#para ter informações sobre a gem instalada = bundle info nome_da_gem
